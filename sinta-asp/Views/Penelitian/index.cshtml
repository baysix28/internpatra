@model List<sinta_asp.Controllers.LowonganKerja>
@{
    ViewData["Title"] = "Dashboard Penelitian";
    
    // Ambil data filter
    var selectedCompany = ViewData["SelectedCompany"] as string;
    var selectedSearch = ViewData["SelectedSearch"] as string;
    var selectedRegion = ViewData["SelectedRegion"] as string;
    var lastScroll = Context.Request.Query["lastScroll"].ToString();
}

<style>

    /* 1. IMPORT FONT BARU */
    @@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@600;700;800&family=Inter:wght@400;500&display=swap');
    
    /* GLOBAL STYLE */
    .penelitian-wrapper { 
        background-color: #fff; color: #333; min-height: 100vh;
        font-family: 'Inter', sans-serif;
    }
    
    /* Reset elemen teks biar gak ngerusak layout Bootstrap */
    .penelitian-wrapper h1, .penelitian-wrapper h2, .penelitian-wrapper p { margin: 0; padding: 0; }

    
    /* 2. HERO SECTION BARU (Tinggi 60% Layar) */
        /* HERO HEADER */
    .hero-section {
        position: relative;
        height: 60vh;
        min-height: 500px;
        
        /* HAPUS LINEAR-GRADIENT DISINI, CUKUP GAMBAR SAJA */
        background-image: url('/img/test.jpg'); /* Pastikan nama file gambar benar */
        background-size: cover;
        background-position: center 10%;
        
        /* Sisanya biarkan sama */
        width: 100vw; 
        left: 50%; right: 50%;
        margin-left: -50vw; margin-right: -50vw;
        margin-bottom: 50px;
        font-family: 'Inter', sans-serif;
        display: flex; align-items: center; /* Pastikan ini ada biar konten vertikal tengah */
    }

        /* 3. GRADIENT ELEGAN (Merah Kiri -> Bening Kanan) */
        .hero-overlay {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(90deg, 
                rgba(139, 0, 0, 0.95) 0%,      /* Kiri: Merah Gelap Pekat */
                rgba(237, 27, 36, 0.7) 45%,    /* Tengah: Merah Pertamina */
                rgba(237, 27, 36, 0.0) 100%    /* Kanan: Bening (Gambar Asli) */
            );
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center; /* Teks di tengah vertikal */
            align-items: flex-start; /* Teks Rata Kiri */
            color: white;
            padding-left: 5%; /* Geser dikit dari pinggir kiri */
        }

        /* 4. TYPOGRAPHY BARU (Poppins & Inter) */
        .hero-title {
            font-family: 'Poppins', sans-serif;
            font-weight: 800;
            font-size: 3.5rem;
            line-height: 1.1;
            margin-bottom: 10px;
            text-shadow: 0 4px 10px rgba(0,0,0,0.3);
        }

        .hero-lead {
            font-family: 'Inter', sans-serif;
            font-size: 1.1rem;
            font-weight: 400;
            
            /* Pakai !important agar tidak kalah sama reset margin */
            margin-bottom: 20px !important; 
            
            opacity: 0.95;
            max-width: 600px;
        }

     /* 5. TOMBOL PREMIUM (ANIMASI FILL KIRI KE KANAN) */
    .btn-apply-hero {
        position: relative; /* Wajib: sebagai jangkar animasi */
        overflow: hidden;   /* Wajib: biar warna tidak bocor keluar sudut melengkung */
        z-index: 1;         /* Pastikan teks ada di layer paling atas */
        
        background-color: transparent; 
        border: 2px solid white;
        
        color: white;
        padding: 15px 40px;
        
        font-family: 'Poppins', sans-serif;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        border-radius: 50px !important;
        text-decoration: none;
        
        /* Transisi warna teks biar halus saat background jadi putih */
        transition: color 0.4s ease; 
        
        display: inline-block;
        margin-top: -10px; /* Jarak dari teks di atasnya */
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    }

    /* INI LAYER PUTIH YANG AKAN BERGERAK */
    .btn-apply-hero::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        
        width: 0%;        /* Mulai dari 0 (kosong) */
        height: 100%;
        
        background-color: white;
        z-index: -1;      /* Posisinya di belakang teks */
        
        /* Animasi gerakan kiri ke kanan (Durasi 0.4 detik) */
        transition: width 0.4s cubic-bezier(0.25, 0.8, 0.25, 1); 
    }

    /* EFEK SAAT MOUSE DI ARAHKAN (HOVER) */
    .btn-apply-hero:hover {
        color: #121c2d; /* Ubah warna teks jadi Biru Gelap biar terbaca */
        /* Kalau mau teks merah Pertamina, ganti jadi color: #ED1B24; */
        border-color: white;
    }

    /* Pemicu Animasi: Lebarkan layer putih jadi 100% */
    .btn-apply-hero:hover::before {
        width: 100%; 
    }

    
    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        
        /* GANTI GRADASI MERAH LAMA DENGAN INI (Dark Blue Slate) */
        /* Kode warna diambil dari sampel gambar referensimu */
        background: linear-gradient(90deg, 
            rgba(20, 35, 50, 0.95) 0%,     /* Kiri: Biru Gelap Pekat (Hampir Hitam) */
            rgba(20, 35, 50, 0.6) 45%,     /* Tengah: Mulai memudar */
            rgba(20, 35, 50, 0.0) 100%     /* Kanan: Transparan (Gambar terlihat) */
        );
        
        z-index: 1;
    }

    .hero-content {
        position: relative;
        z-index: 2;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: white;
        text-align: center;
    }

    /* TYPOGRAPHY BARU */
    .badge-custom {
        background-color: #00A950;
        color: white; padding: 8px 20px; border-radius: 50px;
        text-transform: uppercase; font-weight: 700; font-size: 0.8rem;
        letter-spacing: 1px; margin-bottom: 20px; display: inline-block;
    }
    
    .hero-title { font-size: 4rem; font-weight: 700; margin-bottom: 10px; }
    .hero-lead { font-size: 1.2rem; font-weight: 400; margin-bottom: 40px; }

    /* LAYOUT UTAMA */
    .main-container { max-width: 1200px; margin: 0 auto; padding: 0 20px 80px 20px; }
    .job-board-layout { display: grid; grid-template-columns: 280px 1fr; gap: 50px; }

    /* COMPONENTS */
    .intro-header { display: flex; justify-content: space-between; margin-bottom: 30px; padding-bottom: 30px; border-bottom: 1px solid #eee; }
    .btn-apply {
        background-color: #00a048; color: white; padding: 10px 25px;
        border-radius: 50px; text-decoration: none; font-weight: 600; display: inline-block;
    }
    .filters label { font-size: 0.85rem; color: #888; font-weight: 600; margin-bottom: 8px; display: block; }
    
    /* FORM STYLING */
    .form-control-custom {
        width: 100%; padding: 12px 15px; font-size: 0.95rem;
        border: 1px solid #e0e0e0; border-radius: 8px; outline: none; background: white;
        appearance: none; -webkit-appearance: none; -moz-appearance: none;
    }
    select.form-control-custom {
        background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23d81c22' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e");
        background-repeat: no-repeat; background-position: right 15px center; 
        background-size: 16px; padding-right: 40px; cursor: pointer;
    }
    input.form-control-custom { background-image: none !important; padding-right: 40px; }
    .search-wrapper { position: relative; }
    .search-wrapper i { 
        position: absolute; right: 15px; top: 50%; 
        transform: translateY(-50%); color: #d81c22; pointer-events: none; 
    }

    /* FLIP CARD */
    .flip-card { background: transparent; width: 100%; height: 320px; perspective: 1000px; cursor: pointer; }
    .flip-card-inner {
        position: relative; width: 100%; height: 100%; text-align: center;
        transition: transform 0.6s; transform-style: preserve-3d;
        border-radius: 12px; box-shadow: 0 4px 10px rgba(0,0,0,0.15);
    }
    .flip-card:hover .flip-card-inner { transform: rotateY(180deg); }
    .flip-card-front, .flip-card-back {
        position: absolute; width: 100%; height: 100%;
        -webkit-backface-visibility: hidden; backface-visibility: hidden;
        border-radius: 12px; overflow: hidden;
    }
    .flip-card-front {
        background-size: cover; background-position: center;
        display: flex; flex-direction: column; justify-content: flex-end;
        padding: 20px; text-align: left;
        background-image: linear-gradient(to top, rgba(0,0,0,0.8) 0%, rgba(0,0,0,0) 60%);
    }
    .flip-card-front h3 { color: white; font-size: 1.2rem; font-weight: 700; text-shadow: 0 2px 4px rgba(0,0,0,0.5); }
    .flip-card-back {
        background: white; color: #333; transform: rotateY(180deg);
        display: flex; flex-direction: column; justify-content: center; align-items: center;
        padding: 20px; border: 2px solid #d81c22;
    }
    .flip-card-back h4 { color: #d81c22; margin-bottom: 15px; font-weight: bold; }
    .btn-card-action { margin-top: 20px; background: #d81c22; color: white; padding: 8px 20px; border-radius: 20px; text-decoration: none; font-size: 0.85rem; }

    @@media (max-width: 768px) { .job-board-layout { grid-template-columns: 1fr; } }
</style>

<script>
    if ('scrollRestoration' in history) { history.scrollRestoration = 'manual'; }
</script>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<div class="penelitian-wrapper">
    <header class="hero-section">
        <div class="hero-overlay"></div>

        <div class="container hero-content">
            
            <h1 class="hero-title">READY TO LEVEL UP?</h1>
            
            <p class="hero-lead">Salurkan ide brilian dan ciptakan dampak nyata bersama kami.</p>
            
            <div>
                <a href="#area-lowongan" class="btn-apply-hero">
                    Apply For Opportunities
                </a>
            </div>
        </div>
    </header>
    <div class="main-container" id="area-lowongan">
        <div class="intro-header">
            <div>
                <h2 style="font-size:1.5rem; margin-bottom:5px;">Help us shape the future of energy.</h2>
                <p style="color:#666;">Explore and apply for opportunities that align with your skills.</p>
            </div>
            <a asp-action="Daftar" class="btn-apply">Apply for Opportunities &rarr;</a>
        </div>

        <div class="job-board-layout">
            
            <aside class="filters">
                <a href="/Penelitian" style="text-decoration:none; color:#666; font-size:0.9rem; margin-bottom:15px; display:inline-block;">Clear filters</a>

                <form method="get" asp-action="Index">
                    <input type="hidden" name="lastScroll" id="lastScrollInput" value="" />

                    <div style="margin-bottom: 25px;">
                        <label>Can't find what you looking for?</label>
                        <div class="search-wrapper">
                            <input type="text" id="inputCari" class="form-control-custom" placeholder="Search..." 
                                   value="@selectedSearch" >
                            <i class="fas fa-search"></i>
                        </div>
                    </div>

                    <div style="margin-bottom: 25px;">
                        <label>Filtered by Company</label>
                        <select name="company" id="companySelect" class="form-control-custom" onchange="gantiCompany(this)">
                            <option value="All" selected="@(selectedCompany == "All" ? "selected" : null)">All Companies</option>
                            <option value="PT Kilang Pertamina Internasional (KPI)" 
                                    selected="@(selectedCompany == "PT Kilang Pertamina Internasional (KPI)" ? "selected" : null)">
                                PT Kilang Pertamina Internasional (KPI)
                            </option>
                            <option value="PT Pertamina Patra Niaga" selected="@(selectedCompany == "PT Pertamina Patra Niaga" ? "selected" : null)">
                                PT Pertamina Patra Niaga (C&T)
                            </option>
                        </select>
                    </div>

                    <div style="margin-bottom: 25px;">
                        <label>By Region</label>
                        <select name="region" id="regionSelect" class="form-control-custom" onchange="submitFilter(this.form)">
                        </select>
                    </div>
                </form>
            </aside>

            <main>
                @if (Model == null || Model.Count == 0)
                {
                    <div style="text-align:center; padding: 60px; color:#999; border: 2px dashed #eee; border-radius:15px; background-color:#fafafa;">
                        <i class="fas fa-search fa-3x" style="margin-bottom:15px; color:#ddd;"></i>
                        <h3>Tidak Ada Lowongan</h3>
                        <p>Coba ubah filter atau kata kunci pencarian Anda.</p>
                    </div>
                }
                else
                {
                    <div class="row" id="containerKartu">
                        @foreach (var item in Model)
                        {
                            <div class="col-md-4 mb-4 item-lowongan" style="margin-bottom: 30px !important;">
                                <div class="flip-card">
                                    <div class="flip-card-inner">
                                        
                                        <div class="flip-card-front" style="background-image: linear-gradient(to top, rgba(0,0,0,0.9) 10%, rgba(0,0,0,0) 60%), url('@item.ImageUrl');">
                                            @if (item.Company != null && item.Company.Contains("Kilang"))
                                            {
                                                <span style="background: #00529C; color: white; padding: 5px 12px; border-radius: 4px; font-size: 0.8rem; font-weight: bold; width: fit-content; margin-bottom: 10px;">
                                                    <i class="fas fa-map-marker-alt"></i> @item.Region
                                                </span>
                                            }
                                            else
                                            {
                                                <span style="background: #00a048; color: white; padding: 5px 12px; border-radius: 4px; font-size: 0.8rem; font-weight: bold; width: fit-content; margin-bottom: 10px;">
                                                    <i class="fas fa-map-marker-alt"></i> @item.Region
                                                </span>
                                            }

                                            <h3 style="margin-top: 0; font-size: 1.3rem;">@item.Title</h3>
                                            <small style="color: #ddd; font-weight: 500;">
                                                <i class="fas fa-building me-1"></i> @item.Company
                                            </small>
                                        </div>

                                        <div class="flip-card-back">
                                            <h4 style="color: #d81c22; margin-bottom: 10px;">Tentang Posisi</h4>
                                            <p style="font-size: 0.9rem; color: #555; padding: 0 15px; line-height: 1.5; margin-bottom: 20px;">
                                                @item.Description
                                            </p>
                                            <a asp-action="Daftar" 
                                               asp-route-preCompany="@item.Company" 
                                               asp-route-preRegion="@item.Region" 
                                               asp-route-preTargetLokasi="@item.Title"   
                                               asp-route-preTargetJurusan="@item.Title" 
                                               class="btn-card-action">
                                                Daftar Sekarang <i class="fas fa-arrow-right ms-1"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>

                    <div id="pesanKosong" style="display: none; text-align: center; padding: 40px; margin-top: 20px; color: #666;">
                        <i class="fas fa-search" style="font-size: 3rem; color: #ddd; margin-bottom: 15px;"></i>
                        <h3>Yah, nggak ketemu...</h3>
                        <p>Coba cari pakai kata kunci lain ya.</p>
                    </div>
                }
            </main>
        </div>
    </div>
</div>

<script>
    // --- 1. DATA REGION (KUNCI HARUS SAMA DENGAN DROPDOWN) ---
    const dataRegion = {
        "PT Pertamina Patra Niaga": ["Regional Jatimbalinus", "Regional Jawa Bagian Tengah", "Regional Jawa Bagian Barat", "Regional Kalimantan", "Regional Maluku Papua", "Regional Sumbagut"],
        
        // [PERUBAHAN DISINI] Tambahkan (KPI) biar kebaca
        "PT Kilang Pertamina Internasional (KPI)": ["Refinery Unit VI Balongan"]
    };

    // --- 2. FUNGSI UPDATE TAMPILAN ---
    function updateRegionOptions() {
        const companySelect = document.getElementById('companySelect');
        const regionSelect = document.getElementById('regionSelect');
        const selectedCompany = companySelect.value;
        const currentRegion = '@selectedRegion'; 

        regionSelect.innerHTML = '';
        
        // SELALU Tampilkan opsi All Regions biar gak bingung
        const allOption = document.createElement('option');
        allOption.value = "All"; // Value 'All' biar ditangkap Controller
        allOption.text = "All Regions";
        regionSelect.appendChild(allOption);

        // Cek apakah ada datanya?
        if (selectedCompany && dataRegion[selectedCompany]) {
            dataRegion[selectedCompany].forEach(reg => {
                const option = document.createElement('option');
                option.value = reg; 
                option.text = reg;
                
                if (reg === currentRegion) option.selected = true;
                
                regionSelect.appendChild(option);
            });
        }
    }

    // --- 3. FUNGSI RESET SAAT GANTI COMPANY (PENTING!) ---
    function gantiCompany(selectElement) {
        // Reset Region ke 'All' supaya filter gak bentrok
        document.getElementById('regionSelect').value = "All";
        
        // Simpan posisi scroll (optional)
        document.getElementById('lastScrollInput').value = window.scrollY;

        // Submit form otomatis
        selectElement.form.submit();
    }

    // --- 2. LIVE SEARCH + PESAN KOSONG ---
    document.addEventListener("DOMContentLoaded", function() {
        updateRegionOptions();

        const inputCari = document.getElementById('inputCari');
        const daftarKartu = document.getElementsByClassName('item-lowongan');
        const pesanKosong = document.getElementById('pesanKosong'); // Ambil elemen pesan kosong

        if (inputCari && daftarKartu.length > 0) {
            inputCari.addEventListener('input', function() {
                const filter = inputCari.value.toLowerCase(); 
                let adaYangMuncul = false; // Flag penanda

                for (let i = 0; i < daftarKartu.length; i++) {
                    const kartu = daftarKartu[i];
                    const teks = kartu.innerText.toLowerCase();

                    if (teks.includes(filter)) {
                        kartu.style.setProperty("display", "block", "important"); 
                        kartu.style.marginBottom = "30px";
                        adaYangMuncul = true; // Hore, ada yang cocok!
                    } else {
                        kartu.style.setProperty("display", "none", "important"); 
                    }
                }

                // LOGIKA MUNCULIN PESAN KOSONG
                if (adaYangMuncul) {
                    pesanKosong.style.display = "none"; // Umpetin pesannya
                } else {
                    pesanKosong.style.display = "block"; // Munculin pesannya
                }
            });
        }
    });

    // --- 3. FILTER SUBMIT ---
    function submitFilter(form) {
        document.getElementById('lastScrollInput').value = window.scrollY;
        form.submit();
    }

    (function() {
        var targetScroll = '@lastScroll';
        if (targetScroll && targetScroll !== "") {
            window.scrollTo(0, parseInt(targetScroll));
        }
    })();
</script>