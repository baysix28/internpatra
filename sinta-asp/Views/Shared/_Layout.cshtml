<!DOCTYPE html>
<html lang="id">
<head>
<<<<<<< HEAD
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"]</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">

    <style>
        :root {
            --p-blue: #00549B;
            --p-red: #E30613;
            --sidebar-width: 260px;
            --topbar-height: 70px;
            --bg-light: #f8fafc;
            --font-main: 'Plus Jakarta Sans', sans-serif;
        }

        body {
            font-family: var(--font-main);
            background-color: var(--bg-light);
            overflow-x: hidden;
        }

        /* Sidebar */
        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            background: #ffffff;
            border-right: 1px solid #e2e8f0;
            z-index: 1000;
            padding: 24px;
        }

        .brand-logo {
            display: flex;
            align-items: center;
            gap: 12px;
            padding-bottom: 30px;
            border-bottom: 1px solid #f1f5f9;
            margin-bottom: 20px;
        }

            .brand-logo img {
                height: 35px;
            }

            .brand-logo span {
                font-weight: 800;
                font-size: 18px;
                color: var(--p-blue);
            }

        .nav-link-custom {
            display: flex;
            align-items: center;
            padding: 12px 16px;
            color: #64748b;
            font-weight: 600;
            font-size: 14px;
            border-radius: 12px;
            text-decoration: none !important;
            margin-bottom: 4px;
        }

            .nav-link-custom:hover, .nav-link-custom.active {
                background: rgba(0, 84, 155, 0.05);
                color: var(--p-blue);
            }

            .nav-link-custom.active {
                background: var(--p-blue);
                color: #fff;
            }

        /* Topbar */
        .topbar {
            height: var(--topbar-height);
            position: fixed;
            top: 0;
            right: 0;
            left: var(--sidebar-width);
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid #e2e8f0;
            z-index: 999;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 30px;
        }

        /* Content Area */
        .main-content {
            margin-left: var(--sidebar-width);
            padding-top: var(--topbar-height);
            min-height: 100vh;
        }

        .stat-card {
            border: none;
            border-radius: 20px;
            background: #fff;
        }

        .badge-soft {
            padding: 6px 12px;
            border-radius: 8px;
            font-weight: 700;
            font-size: 11px;
        }

        .bg-process {
            background: #e0f2fe;
            color: #0369a1;
=======
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>@ViewData["Title"] - SINTA Pertamina</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* Penambahan CSS agar body memenuhi layar */
        html, body {
            height: 100%;
            margin: 0;
        }

        /* Flexbox untuk memaksa footer ke bawah */
        body { 
            display: flex;
            flex-direction: column;
            background-color: #ffffff; 
        }

        .top-bar { border-bottom: 50px solid #e74c3c; padding: 15px 30px; background: white; }
        .logo-link { text-decoration: none; cursor: pointer; transition: transform 0.3s ease; display: inline-block; }
        .logo-link:hover { transform: scale(1.05); }

        /* Main akan mengambil sisa ruang yang kosong */
        main {
            flex: 1 0 auto;
        }

        footer { 
            flex-shrink: 0; /* Mencegah footer gepeng */
            border-top: 1px solid #ddd; 
            padding: 20px; 
            font-size: 16px; 
            text-align: center; 
            color: #555; 
            background: #f1f1f1; 
>>>>>>> 659a81f9878d152c3c8220b7520b93e73f755cfb
        }
    </style>
</head>
<body>

<<<<<<< HEAD
    <nav class="sidebar">
        <div class="brand-logo">
            <img src="~/images/logo-pertamina.png" alt="Logo">
            <span>SINTA</span>
        </div>
        <ul class="nav-menu list-unstyled">
            <li class="small font-weight-bold text-muted mb-2">MENU UTAMA</li>
            <li>
                <a asp-page="/Index" class="nav-link-custom">
                    <i class="fa-solid fa-house mr-2"></i> Dashboard
                </a>
            </li>
            <li>
                <a asp-page="/Mahasiswa/Index" class="nav-link-custom">
                    <i class="fa-solid fa-users mr-2"></i> Data Mahasiswa
                </a>
            </li>
        </ul>
    </nav>

    <header class="topbar">
        <div class="font-weight-bold">Panel Admin</div>
        <div class="d-flex align-items-center" style="gap: 15px;">
            <div class="text-right d-none d-sm-block">
                <div class="font-weight-bold small">Admin SINTA</div>
                <small class="text-success">Online</small>
            </div>
            <img src="https://ui-avatars.com/api/?name=Admin" class="rounded-circle" width="35">
        </div>
    </header>

    <main class="main-content">
        <div class="container-fluid py-4">
            @RenderBody() @* KONTEN HALAMAN AKAN MUNCUL DI SINI *@
        </div>
    </main>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("Scripts", required: false)
=======
<div class="top-bar d-flex justify-content-between align-items-center">
    <a href="@Url.Action("Index", "Home")" class="logo-link">
        <img src="~/img/logo-pertaminaa.png" height="70" alt="Pertamina Logo">
    </a>
    <a href="@Url.Action("Index", "Home")" class="logo-link">
        <img src="~/img/logo-sinta.png" height="70" alt="SINTA Logo">
    </a>
</div>

<main role="main">
    @RenderBody() 
</main>

<footer>
    <p style="margin: 0; color: #555;">&copy; 2026 PT. Pertamina (Persero) | All Rights Reserved. | Contact Us : 135</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
@await RenderSectionAsync("Scripts", required: false)
>>>>>>> 659a81f9878d152c3c8220b7520b93e73f755cfb
</body>
</html>